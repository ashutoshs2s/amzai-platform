import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// Configuration for building this Next.js app into a Cloudflare Worker.
// Defaults for now. Caching and incremental static regeneration options get
// added here when there is something to cache.
export default defineCloudflareConfig();
